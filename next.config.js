/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'is1-ssl.mzstatic.com',
      'upload.wikimedia.org',
      'github.com',
      'preview.redd.it',
    ],
  },
}

module.exports = nextConfig
